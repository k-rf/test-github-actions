name: "test"

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: k-rf/custom-github-actions/app/modules/example
      - uses: k-rf/custom-github-actions/app/modules/sample
